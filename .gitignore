# Build results
bin/
obj/
*.user
*.suo
*.cache
*.csproj.user

# Visual Studio
.vs/
.vscode/
.idea/
*.sln.DotSettings
*.log

# Credentials: plaintext config (dev only, never commit)
**/appsettings.json

# Legacy credentials file (replaced by CredentialService + appsettings.enc)
**/Credentials.cs

# Local database (don't sync)
Documents/VANTAGE/VANTAGE_Local.db

# Procore tokens (stored locally per user)
**/procore_tokens.json

# Publish outputs (generated by publish scripts)
VANTAGE-*.zip
VANTAGE-Setup.exe.claude/
