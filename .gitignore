# Build results
bin/
obj/
*.user
*.suo
*.cache
*.csproj.user

# Visual Studio
.vs/
.vscode/
.idea/
*.sln.DotSettings
*.log

# Credentials: plaintext config (dev only, never commit)
**/appsettings.json

# Legacy credentials file (replaced by CredentialService + appsettings.enc)
**/Credentials.cs

# Local database (don't sync)
Documents/VANTAGE/VANTAGE_Local.db

# Procore tokens (stored locally per user)
**/procore_tokens.json

# Procore credentials/notes + install-key hand-off (local only — never commit)
Plans/ProcoreInfo.txt
Plans/Procore_Admin_Install_Instructions.md

# Publish outputs (generated by publish scripts)
VANTAGE-*.zip
VANTAGE-Setup.exe
.claude/settings.local.json
Plans/~$rateSheet.xlsx
~$*

# Codex local artifact-runtime junction (generated, never project source)
.codex_tmp/

# Visual Studio upgrade artifacts (regenerated by VS, no value in source control)
[Bb]ackup*/
UpgradeLog*.htm

# MCAA rate DB — regenerable build artifact (Scripts/export_rates_to_sqlite.py); S3-delivered, not bundled
Resources/cdx_weblem_rates.db

# Client/test isometrics live on the G drive, not in git
Plans/MCAA-Takeoff/vantage_handoff/drawings/**/*.pdf
