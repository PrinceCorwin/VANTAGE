<UserControl x:Class="VANTAGE.Views.SidePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{StaticResource ControlBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with tabs and close button -->
        <Border Grid.Row="0" 
                Background="{StaticResource DarkestBackgroundColor}" 
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Tab Buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="btnHelpTab"
                            Content="Help"
                            Padding="15,6"
                            Margin="0,0,5,0"
                            Background="{StaticResource AccentColor}"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            Click="BtnHelpTab_Click"
                            Cursor="Hand"/>
                    <Button x:Name="btnAiTab"
                            Content="AI Assistant"
                            Padding="15,6"
                            Background="Transparent"
                            Foreground="{StaticResource ForegroundColor}"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            Click="BtnAiTab_Click"
                            Cursor="Hand"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1"
                        Content="✕"
                        Width="30"
                        Height="30"
                        Background="Transparent"
                        Foreground="{StaticResource ForegroundColor}"
                        BorderThickness="0"
                        FontSize="14"
                        Click="BtnClose_Click"
                        ToolTip="Close sidebar"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Context Header -->
        <Border Grid.Row="1"
                Background="{StaticResource BackgroundColor}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="10,6">
            <TextBlock x:Name="txtContextHeader"
                       Text="Getting Started"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource AccentColor}"/>
        </Border>

        <!-- Help Content (WebView2) -->
        <Grid x:Name="gridHelpContent" Grid.Row="2" Visibility="Visible">
            <wv2:WebView2 x:Name="webViewHelp"
                          DefaultBackgroundColor="Transparent"/>

            <!-- Loading/Error overlay -->
            <Border x:Name="helpOverlay"
                    Background="{StaticResource ControlBackground}"
                    Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock x:Name="txtHelpStatus"
                               Text="Loading help content..."
                               Foreground="{StaticResource TextColorSecondary}"
                               FontSize="14"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- AI Content (placeholder for future) -->
        <Grid x:Name="gridAiContent" Grid.Row="2" Visibility="Collapsed">
            <Border Background="{StaticResource BackgroundColor}" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Chat history area -->
                    <Border Grid.Row="0"
                            Background="{StaticResource ControlBackground}"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Margin="0,0,0,10">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                            <StackPanel x:Name="chatHistory">
                                <TextBlock Text="AI Assistant coming soon..."
                                           Foreground="{StaticResource TextColorSecondary}"
                                           FontStyle="Italic"
                                           TextWrapping="Wrap"
                                           Margin="0,10"/>
                                <TextBlock Text="This feature will allow you to ask questions about MILESTONE, get help with tasks, and receive AI-powered suggestions based on your current context."
                                           Foreground="{StaticResource TextColorSecondary}"
                                           TextWrapping="Wrap"
                                           Margin="0,10"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Input area (disabled for now) -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="txtAiInput"
                                 Grid.Column="0"
                                 Height="35"
                                 Padding="10,8"
                                 Background="{StaticResource ControlBackground}"
                                 Foreground="{StaticResource ForegroundColor}"
                                 BorderBrush="{StaticResource BorderColor}"
                                 BorderThickness="1"
                                 IsEnabled="False"
                                 Text="AI chat coming soon..."/>

                        <Button Grid.Column="1"
                                Content="Send"
                                Padding="15,8"
                                Margin="10,0,0,0"
                                Background="{StaticResource AccentColor}"
                                Foreground="White"
                                BorderThickness="0"
                                IsEnabled="False"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>