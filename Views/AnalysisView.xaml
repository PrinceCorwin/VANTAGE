<UserControl x:Class="VANTAGE.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:converters="clr-namespace:VANTAGE.Converters"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="{DynamicResource BackgroundColor}">

    <UserControl.Resources>
        <!-- Default TextBlock style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
        </Style>

        <!-- Converter for % Complete cell coloring -->
        <converters:PercentToColorConverter x:Key="PercentToColorConverter"/>

        <!-- CellStyle for % Complete column with conditional coloring -->
        <Style x:Key="PercentColorStyle" TargetType="syncfusion:GridCell" BasedOn="{StaticResource {x:Type syncfusion:GridCell}}">
            <Setter Property="Background" Value="{Binding PercentComplete, Converter={StaticResource PercentToColorConverter}}"/>
        </Style>

        <!-- GridSplitter styles - thin modern look with centered grip -->
        <Style x:Key="VerticalGridSplitterStyle" TargetType="GridSplitter">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="SizeWE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Grid Background="Transparent">
                            <!-- Thin vertical line -->
                            <Rectangle Width="1" Fill="{DynamicResource SplitterDots}" Opacity="0.4" HorizontalAlignment="Center"/>
                            <!-- Centered grip dots -->
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="0,1"/>
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="0,1"/>
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="0,1"/>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HorizontalGridSplitterStyle" TargetType="GridSplitter">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="SizeNS"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Grid Background="Transparent">
                            <!-- Thin horizontal line -->
                            <Rectangle Height="1" Fill="{DynamicResource SplitterDots}" Opacity="0.4" VerticalAlignment="Center"/>
                            <!-- Centered grip dots -->
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="1,0"/>
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="1,0"/>
                                <Ellipse Width="3" Height="3" Fill="{DynamicResource SplitterDots}" Opacity="0.6" Margin="1,0"/>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section border style -->
        <Style x:Key="SectionBorderStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <!-- Custom scrollbar thumb for filter panel -->
        <ControlTemplate x:Key="FilterThumbTemplate" TargetType="Thumb">
            <Border x:Name="thumbBorder"
                    Background="{DynamicResource ScrollBarThumbColor}"
                    BorderBrush="{DynamicResource ScrollBarBorderColor}"
                    BorderThickness="1"
                    CornerRadius="2"/>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource ScrollBarThumbHoverColor}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Custom ScrollViewer with themed scrollbar -->
        <Style x:Key="FilterScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1"
                                       Orientation="Vertical" Width="14" MinWidth="14"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}">
                                <ScrollBar.Template>
                                    <ControlTemplate TargetType="ScrollBar">
                                        <Grid Background="{DynamicResource ScrollBarTrackColor}">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <RepeatButton Grid.Row="0" Command="ScrollBar.LineUpCommand"
                                                          Height="14" Opacity="0">
                                                <RepeatButton.Template>
                                                    <ControlTemplate><Border Height="14"/></ControlTemplate>
                                                </RepeatButton.Template>
                                            </RepeatButton>
                                            <Track Grid.Row="1" x:Name="PART_Track" IsDirectionReversed="True">
                                                <Track.Thumb>
                                                    <Thumb Template="{StaticResource FilterThumbTemplate}"/>
                                                </Track.Thumb>
                                            </Track>
                                            <RepeatButton Grid.Row="2" Command="ScrollBar.LineDownCommand"
                                                          Height="14" Opacity="0">
                                                <RepeatButton.Template>
                                                    <ControlTemplate><Border Height="14"/></ControlTemplate>
                                                </RepeatButton.Template>
                                            </RepeatButton>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollBar.Template>
                            </ScrollBar>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main Grid with independent row grids -->
    <Grid x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="row0" Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition x:Name="row1" Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Row Grid - independent column sizing -->
        <Grid x:Name="topRowGrid" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="topCol0" Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="topCol1" Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="topCol2" Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="topCol3" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Top Row Vertical GridSplitters -->
            <GridSplitter Grid.Column="1"
                          Style="{StaticResource VerticalGridSplitterStyle}"
                          DragCompleted="GridSplitter_DragCompleted"
                          ToolTip="Drag to resize columns"/>
            <GridSplitter Grid.Column="3"
                          Style="{StaticResource VerticalGridSplitterStyle}"
                          DragCompleted="GridSplitter_DragCompleted"
                          ToolTip="Drag to resize columns"/>
            <GridSplitter Grid.Column="5"
                          Style="{StaticResource VerticalGridSplitterStyle}"
                          DragCompleted="GridSplitter_DragCompleted"
                          ToolTip="Drag to resize columns"/>

            <!-- Section (1,1) - Chart Filters -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Chart Filters" FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource ForegroundColor}" Margin="8,6,8,4"/>

                    <ScrollViewer Grid.Row="1" Style="{StaticResource FilterScrollViewerStyle}" Margin="8,0,8,8">
                        <StackPanel>
                            <TextBlock Text="Area" Foreground="{DynamicResource ForegroundColor}" Margin="0,4,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_Area" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by Area"/>

                            <TextBlock Text="Aux1" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_Aux1" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by Aux1"/>

                            <TextBlock Text="Aux2" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_Aux2" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by Aux2"/>

                            <TextBlock Text="Aux3" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_Aux3" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by Aux3"/>

                            <TextBlock Text="CompType" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_CompType" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by CompType"/>

                            <TextBlock Text="DwgNO" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_DwgNO" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by DwgNO"/>

                            <TextBlock Text="PhaseCategory" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_PhaseCategory" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by PhaseCategory"/>

                            <TextBlock Text="PhaseCode" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_PhaseCode" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by PhaseCode"/>

                            <TextBlock Text="ProjectID" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_ProjectID" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by ProjectID"/>

                            <TextBlock Text="ROCStep" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_ROCStep" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by ROCStep"/>

                            <TextBlock Text="SchedActNO" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_SchedActNO" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by SchedActNO"/>

                            <TextBlock Text="WorkPackage" Foreground="{DynamicResource ForegroundColor}" Margin="0,6,0,2" FontSize="11"/>
                            <syncfusion:ComboBoxAdv x:Name="cmbFilter_WorkPackage" AllowMultiSelect="True" EnableOKCancel="False"
                                                    SelectionChanged="ChartFilter_SelectionChanged" ToolTip="Filter charts by WorkPackage"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Section (1,2) - Dynamic Visual -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Visual selection controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8,6" VerticalAlignment="Center">
                        <ComboBox x:Name="cmbVisualType_1_1"
                                  MinWidth="110"
                                  SelectionChanged="CmbVisualType_1_1_SelectionChanged"
                                  ToolTip="Select visualization type"/>
                        <ComboBox x:Name="cmbXAxis_1_1"
                                  MinWidth="100"
                                  Margin="8,0,0,0"
                                  SelectionChanged="CmbXAxis_1_1_SelectionChanged"
                                  ToolTip="Select grouping field for X axis"/>
                        <ComboBox x:Name="cmbDataField_1_1"
                                  MinWidth="100"
                                  Margin="8,0,0,0"
                                  SelectionChanged="CmbDataField_1_1_SelectionChanged"
                                  ToolTip="Select numeric field for Y axis"/>
                    </StackPanel>

                    <!-- Dynamic visual content -->
                    <ContentControl x:Name="visualHost_1_1" Grid.Row="1"/>
                </Grid>
            </Border>

            <!-- Section (1,3) -->
            <Border Grid.Column="4" Style="{StaticResource SectionBorderStyle}">
                <TextBlock Text="Section 1,3" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Section (1,4) -->
            <Border Grid.Column="6" Style="{StaticResource SectionBorderStyle}">
                <TextBlock Text="Section 1,4" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>

        <!-- Horizontal GridSplitter -->
        <GridSplitter Grid.Row="1"
                      Style="{StaticResource HorizontalGridSplitterStyle}"
                      DragCompleted="GridSplitter_DragCompleted"
                      ToolTip="Drag to resize rows"/>

        <!-- Bottom Row Grid - independent column sizing (3 sections) -->
        <Grid x:Name="bottomRowGrid" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="bottomCol0" Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="bottomCol1" Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="bottomCol2" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bottom Row Vertical GridSplitters -->
            <GridSplitter Grid.Column="1"
                          Style="{StaticResource VerticalGridSplitterStyle}"
                          DragCompleted="GridSplitter_DragCompleted"
                          ToolTip="Drag to resize columns"/>
            <GridSplitter Grid.Column="3"
                          Style="{StaticResource VerticalGridSplitterStyle}"
                          DragCompleted="GridSplitter_DragCompleted"
                          ToolTip="Drag to resize columns"/>

            <!-- Section (2,1) - SUMMARY GRID -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Filter Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8" VerticalAlignment="Center">
                        <!-- Group By -->
                        <TextBlock Text="Group By:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="cmbGroupBy"
                                  MinWidth="120"
                                  SelectionChanged="CmbGroupBy_SelectionChanged"
                                  ToolTip="Select field to group activities by"/>

                        <Rectangle Width="1" Fill="{DynamicResource ControlBorder}" Margin="12,2"/>

                        <!-- User Filter -->
                        <RadioButton x:Name="rbCurrentUser" Content="My Records"
                                     VerticalAlignment="Center" Margin="8,0"
                                     Foreground="{DynamicResource ForegroundColor}"
                                     Checked="UserFilter_Changed"
                                     ToolTip="Show only records assigned to you"/>
                        <RadioButton x:Name="rbAllUsers" Content="All Users"
                                     VerticalAlignment="Center" Margin="8,0"
                                     Foreground="{DynamicResource ForegroundColor}"
                                     Checked="UserFilter_Changed"
                                     ToolTip="Show records from all users"/>

                        <Rectangle Width="1" Fill="{DynamicResource ControlBorder}" Margin="12,2"/>

                        <!-- Project Filter (multi-select) -->
                        <TextBlock Text="Projects:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <syncfusion:ComboBoxAdv x:Name="cmbProjects"
                                                MinWidth="90"
                                                AllowMultiSelect="True"
                                                EnableOKCancel="False"
                                                SelectionChanged="CmbProjects_SelectionChanged"
                                                ToolTip="Select projects to include in analysis"/>

                        <Rectangle Width="1" Fill="{DynamicResource ControlBorder}" Margin="12,2"/>

                        <Button Content="Export"
                                Click="BtnExport_Click"
                                Padding="8,2"
                                ToolTip="Export current grid data to Excel"/>
                    </StackPanel>

                    <!-- Summary Grid -->
                    <syncfusion:SfDataGrid x:Name="summaryGrid" Grid.Row="1"
                                           AutoGenerateColumns="False"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           AllowDraggingColumns="True"
                                           AllowResizingColumns="True"
                                           IsReadOnly="True"
                                           SelectionMode="Single"
                                           NavigationMode="Row"
                                           ColumnSizer="Star"
                                           HeaderRowHeight="32"
                                           RowHeight="28"
                                           GridLinesVisibility="Both"
                                           HeaderLinesVisibility="Both">
                        <syncfusion:SfDataGrid.HeaderStyle>
                            <Style TargetType="syncfusion:GridHeaderCellControl">
                                <Setter Property="Background" Value="{DynamicResource GridHeaderBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource GridHeaderForeground}"/>
                            </Style>
                        </syncfusion:SfDataGrid.HeaderStyle>
                        <syncfusion:SfDataGrid.Resources>
                            <Style TargetType="syncfusion:FilterToggleButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="syncfusion:FilterToggleButton">
                                            <Grid SnapsToDevicePixels="True" Background="Transparent">
                                                <VisualStateManager.VisualStateGroups>
                                                    <VisualStateGroup x:Name="FilterStates">
                                                        <VisualState x:Name="Filtered">
                                                            <Storyboard>
                                                                <ColorAnimation Storyboard.TargetName="IconStroke"
                                                                                Storyboard.TargetProperty="Color"
                                                                                To="{StaticResource FilterIconActiveColor}"
                                                                                Duration="0:0:0.3"/>
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="UnFiltered">
                                                            <Storyboard>
                                                                <ColorAnimation Storyboard.TargetName="IconStroke"
                                                                                Storyboard.TargetProperty="Color"
                                                                                To="{StaticResource FilterIconColor}"
                                                                                Duration="0:0:0.3"/>
                                                            </Storyboard>
                                                        </VisualState>
                                                    </VisualStateGroup>
                                                </VisualStateManager.VisualStateGroups>
                                                <Path Data="M 1,2 L 11,2 L 7,6 L 7,10 L 5,10 L 5,6 Z"
                                                      Stretch="Uniform" Width="10" Height="10"
                                                      StrokeThickness="1.2" Fill="Transparent" Margin="3">
                                                    <Path.Stroke>
                                                        <SolidColorBrush x:Name="IconStroke" Color="{StaticResource FilterIconColor}"/>
                                                    </Path.Stroke>
                                                </Path>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </syncfusion:SfDataGrid.Resources>
                        <syncfusion:SfDataGrid.Columns>
                            <syncfusion:GridTextColumn x:Name="colGroupValue"
                                                       MappingName="GroupValue"
                                                       HeaderText="Group"
                                                       AllowFiltering="True"
                                                       AllowSorting="True"
                                                       Width="150"/>
                            <syncfusion:GridNumericColumn MappingName="BudgetMHs"
                                                          HeaderText="BudgetMHs"
                                                          NumberDecimalDigits="1"
                                                          AllowFiltering="True"
                                                          AllowSorting="True"/>
                            <syncfusion:GridNumericColumn MappingName="EarnedMHs"
                                                          HeaderText="EarnedMHs"
                                                          NumberDecimalDigits="1"
                                                          AllowFiltering="True"
                                                          AllowSorting="True"/>
                            <syncfusion:GridNumericColumn MappingName="Quantity"
                                                          HeaderText="Quantity"
                                                          NumberDecimalDigits="1"
                                                          AllowFiltering="True"
                                                          AllowSorting="True"/>
                            <syncfusion:GridNumericColumn MappingName="QtyEarned"
                                                          HeaderText="QtyEarned"
                                                          NumberDecimalDigits="1"
                                                          AllowFiltering="True"
                                                          AllowSorting="True"/>
                            <syncfusion:GridNumericColumn MappingName="PercentComplete"
                                                          HeaderText="% Complete"
                                                          NumberDecimalDigits="1"
                                                          AllowFiltering="True"
                                                          AllowSorting="True"
                                                          CellStyle="{StaticResource PercentColorStyle}"/>
                        </syncfusion:SfDataGrid.Columns>
                    </syncfusion:SfDataGrid>
                </Grid>
            </Border>

            <!-- Section (2,2) -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorderStyle}">
                <TextBlock Text="Section 2,2" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Section (2,3) -->
            <Border Grid.Column="4" Style="{StaticResource SectionBorderStyle}">
                <TextBlock Text="Section 2,3" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
