
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- === FONT SETTINGS === -->
    <FontFamily x:Key="FontFamilyPrimary">Segoe UI</FontFamily>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="FontSizeNormal">14</sys:Double>

    <!-- === COLOR PALETTE === -->
    <SolidColorBrush x:Key="BackgroundColor" Color="#FFF6F0FA"/>
    <SolidColorBrush x:Key="DarkBackgroundColor" Color="#FFE8DCF0"/>
    <SolidColorBrush x:Key="DarkestBackgroundColor" Color="#FF2D1B36"/>
    <SolidColorBrush x:Key="ForegroundColor" Color="#FF2D1B36"/>
    <SolidColorBrush x:Key="TextColorSecondary" Color="#FF7B6B8A"/>
    <SolidColorBrush x:Key="AccentColor" Color="#FFE040FB"/>
    <SolidColorBrush x:Key="BorderColor" Color="#FFD4C4E0"/>
    <SolidColorBrush x:Key="DisabledColor" Color="#FFBCAFC8"/>

    <!-- === WINDOW ELEMENTS === -->
    <SolidColorBrush x:Key="WindowBackground" Color="#FFFBF7FD"/>
    <SolidColorBrush x:Key="ControlBackground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ControlBackgroundGreen" Color="#FF1B3D1B"/>
    <SolidColorBrush x:Key="ControlBackgroundRed" Color="#FF3D1B1B"/>
    <SolidColorBrush x:Key="ControlForeground" Color="#FF2D1B36"/>
    <SolidColorBrush x:Key="ControlBorder" Color="#FFD4C4E0"/>
    <SolidColorBrush x:Key="ControlHoverBackground" Color="#FFEDE4F5"/>
    <SolidColorBrush x:Key="ControlPressedBackground" Color="#FFD4C4E0"/>

    <!-- === DATAGRID COLORS === -->
    <SolidColorBrush x:Key="GridHeaderBackground" Color="#FF4A148C"/>

    <!-- === STATUS COLORS === -->
    <SolidColorBrush x:Key="StatusGreen" Color="#FF27AE60"/>
    <SolidColorBrush x:Key="StatusYellow" Color="#FF8B7D26"/>
    <SolidColorBrush x:Key="StatusYellowBg" Color="#FFF5F0D0"/>
    <SolidColorBrush x:Key="StatusRed" Color="#FFB63434"/>
    <SolidColorBrush x:Key="StatusRedBg" Color="#FFFDE8E8"/>
    <SolidColorBrush x:Key="StatusInProgress" Color="#FF7B1FA2"/>
    <SolidColorBrush x:Key="StatusNotStarted" Color="#FF95A5A6"/>

    <!-- === ACTIVE FILTER BORDER === -->
    <SolidColorBrush x:Key="ActiveFilterBorderColor" Color="#FFE040FB"/>

    <!-- === TOOLBAR COLORS === -->
    <SolidColorBrush x:Key="ToolbarBackground" Color="#FFEDE4F5"/>

    <!-- === STATUSBAR COLORS === -->
    <SolidColorBrush x:Key="StatusBarBackground" Color="#FFF0E8F5"/>
    <SolidColorBrush x:Key="StatusBarForeground" Color="#FF5A3D6E"/>

    <!-- === NON-OWNED RECORDS COLORS === -->
    <SolidColorBrush x:Key="NotOwnedRowBackground" Color="#FFF3E8FF"/>
    <SolidColorBrush x:Key="NotOwnedRowForeground" Color="#FF7B5FA0"/>

    <!-- === ACTION BUTTON COLORS === -->
    <!-- Green buttons: Add, Submit, Save, Restore -->
    <SolidColorBrush x:Key="ButtonSuccessBackground" Color="#FF2D7D46"/>
    <SolidColorBrush x:Key="ButtonSuccessBorder" Color="#FF2D7D46"/>
    <SolidColorBrush x:Key="ButtonSuccessHover" Color="#FF359954"/>

    <!-- Red buttons: Delete, Remove, Destructive actions -->
    <SolidColorBrush x:Key="ButtonDangerBackground" Color="#FFB33A3A"/>
    <SolidColorBrush x:Key="ButtonDangerBorder" Color="#FFB33A3A"/>
    <SolidColorBrush x:Key="ButtonDangerHover" Color="#FFCC4444"/>

    <!-- Purple buttons: Primary actions -->
    <SolidColorBrush x:Key="ButtonPrimaryBackground" Color="#FF7B1FA2"/>
    <SolidColorBrush x:Key="ButtonPrimaryBorder" Color="#FF7B1FA2"/>
    <SolidColorBrush x:Key="ButtonPrimaryHover" Color="#FF9C27B0"/>

    <!-- === OVERLAY COLORS === -->
    <SolidColorBrush x:Key="OverlayBackground" Color="#AAFFFFFF"/>
    <SolidColorBrush x:Key="OverlayText" Color="#FF2D1B36"/>
    <SolidColorBrush x:Key="OverlayTextSecondary" Color="#FF7B6B8A"/>
    <!-- Note: TrackColor on SfLinearProgressBar doesn't accept resource binding -->

    <!-- === ERROR/WARNING COLORS === -->
    <SolidColorBrush x:Key="ErrorText" Color="#FFE74C3C"/>
    <SolidColorBrush x:Key="WarningText" Color="#FFD4A017"/>
    <SolidColorBrush x:Key="WarningHighlight" Color="#FFFF6B6B"/>
    <SolidColorBrush x:Key="WarningHighlightBackground" Color="#44FF6B6B"/>

    <!-- === UI ELEMENT COLORS === -->
    <SolidColorBrush x:Key="SidebarBackground" Color="#FFF3EDF7"/>
    <SolidColorBrush x:Key="SidebarBorder" Color="#FFD4C4E0"/>
    <SolidColorBrush x:Key="DividerColor" Color="#FFD4C4E0"/>
    <SolidColorBrush x:Key="SplitterDots" Color="#FFBCAFC8"/>
    <SolidColorBrush x:Key="DisabledText" Color="#FFBCAFC8"/>

    <!-- === TOOLBAR BUTTON COLORS === -->
    <SolidColorBrush x:Key="ToolbarForeground" Color="#E8EAED"/>
    <SolidColorBrush x:Key="ToolbarHoverBackground" Color="#FF5C1780"/>
    <SolidColorBrush x:Key="ToolbarHoverForeground" Color="#FFFFFFFF"/>

    <!-- === GRID HEADER COLORS === -->
    <SolidColorBrush x:Key="GridHeaderForeground" Color="#FFE0D0F0"/>
    <Color x:Key="FilterIconColor">#FFD0C0E0</Color>
    <Color x:Key="FilterIconActiveColor">#FFFF4444</Color>

    <!-- === ACTION BUTTON COLORS === -->
    <SolidColorBrush x:Key="ActionButtonForeground" Color="#FFE8EAED"/>

    <!-- ========================================= -->
    <!-- === DEFAULT BUTTON STYLE === -->
    <!-- ========================================= -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyPrimary}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Focusable="False"
                                    RecognizesAccessKey="True"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ControlHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ControlBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================= -->
    <!-- === TOOLBAR BUTTON STYLE === -->
    <!-- ========================================= -->
    <Style x:Key="ToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ToolbarForeground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyPrimary}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Focusable="False"
                                    RecognizesAccessKey="True"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ToolbarHoverBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource ToolbarHoverForeground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
