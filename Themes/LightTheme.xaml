
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- === FONT SETTINGS === -->
    <FontFamily x:Key="FontFamilyPrimary">Segoe UI</FontFamily>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="FontSizeNormal">14</sys:Double>

    <!-- === COLOR PALETTE === -->
    <SolidColorBrush x:Key="BackgroundColor" Color="#FFF3F3F3"/>
    <SolidColorBrush x:Key="DarkBackgroundColor" Color="#FFE0E0E0"/>
    <SolidColorBrush x:Key="DarkestBackgroundColor" Color="#161616"/>
    <SolidColorBrush x:Key="ForegroundColor" Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="TextColorSecondary" Color="#FF666666"/>
    <SolidColorBrush x:Key="AccentColor" Color="#FF0078D7"/>
    <SolidColorBrush x:Key="BorderColor" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="DisabledColor" Color="#FFB0B0B0"/>

    <!-- === WINDOW ELEMENTS === -->
    <SolidColorBrush x:Key="WindowBackground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ControlBackground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ControlBackgroundGreen" Color="#012a00"/>
    <SolidColorBrush x:Key="ControlBackgroundRed" Color="#2a0000"/>
    <SolidColorBrush x:Key="ControlForeground" Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="ControlBorder" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="ControlHoverBackground" Color="#FFE8E8E8"/>
    <SolidColorBrush x:Key="ControlPressedBackground" Color="#FFD0D0D0"/>

    <!-- === DATAGRID COLORS === -->
    <SolidColorBrush x:Key="GridHeaderBackground" Color="#002234"/>

    <!-- === STATUS COLORS === -->
    <SolidColorBrush x:Key="StatusGreen" Color="#FF27AE60"/>
    <SolidColorBrush x:Key="StatusGreenBgBtn" Color="#062010"/>
    <SolidColorBrush x:Key="StatusGreenFgBtn" Color="#FF3DD975"/>
    <SolidColorBrush x:Key="StatusYellow" Color="#5b4b26"/>
    <SolidColorBrush x:Key="StatusYellowBg" Color="#5b4b26"/>
    <SolidColorBrush x:Key="StatusYellowFg" Color="#E8EAED"/>
    <SolidColorBrush x:Key="StatusRed" Color="#b63434"/>
    <SolidColorBrush x:Key="StatusRedBg" Color="#5b2626"/>
    <SolidColorBrush x:Key="StatusRedBgBtn" Color="#200808"/>
    <SolidColorBrush x:Key="StatusRedFgBtn" Color="#FFDC5050"/>
    <SolidColorBrush x:Key="StatusGoldBg" Color="#fff8c4"/>
    <SolidColorBrush x:Key="StatusInProgress" Color="#b63434"/>
    <SolidColorBrush x:Key="StatusInProgressBgBtn" Color="#201800"/>
    <SolidColorBrush x:Key="StatusInProgressFgBtn" Color="#FFFFC830"/>
    <SolidColorBrush x:Key="StatusNotStarted" Color="#FF95A5A6"/>

    <!-- === ACTIVE FILTER BORDER === -->
    <SolidColorBrush x:Key="ActiveFilterBorderColor" Color="#FF27AE60"/>

    <!-- === TOOLBAR COLORS === -->
    <SolidColorBrush x:Key="ToolbarBackground" Color="#FFEAEAEA"/>

    <!-- === STATUSBAR COLORS === -->
    <SolidColorBrush x:Key="StatusBarBackground" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="StatusBarForeground" Color="#FF555555"/>

    <!-- === NON-OWNED RECORDS COLORS === -->
    <SolidColorBrush x:Key="NotOwnedRowBackground" Color="#FFF0F4FF"/>
    <SolidColorBrush x:Key="NotOwnedRowForeground" Color="#FF888888"/>

    <!-- === ACTION BUTTON COLORS === -->
    <!-- Green buttons: Add, Submit, Save, Restore -->
    <SolidColorBrush x:Key="ButtonSuccessBackground" Color="#2D7D46"/>
    <SolidColorBrush x:Key="ButtonSuccessBorder" Color="#2D7D46"/>
    <SolidColorBrush x:Key="ButtonSuccessHover" Color="#359954"/>

    <!-- Red buttons: Delete, Remove, Destructive actions -->
    <SolidColorBrush x:Key="ButtonDangerBackground" Color="#B33A3A"/>
    <SolidColorBrush x:Key="ButtonDangerBorder" Color="#B33A3A"/>
    <SolidColorBrush x:Key="ButtonDangerHover" Color="#CC4444"/>

    <!-- Blue buttons: Primary actions -->
    <SolidColorBrush x:Key="ButtonPrimaryBackground" Color="#0078D7"/>
    <SolidColorBrush x:Key="ButtonPrimaryBorder" Color="#0078D7"/>
    <SolidColorBrush x:Key="ButtonPrimaryHover" Color="#1A8BE0"/>

    <!-- === OVERLAY COLORS === -->
    <SolidColorBrush x:Key="OverlayBackground" Color="#AAFFFFFF"/>
    <SolidColorBrush x:Key="OverlayText" Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="OverlayTextSecondary" Color="#FF666666"/>
    <!-- Note: TrackColor on SfLinearProgressBar doesn't accept resource binding -->

    <!-- === ERROR/WARNING COLORS === -->
    <SolidColorBrush x:Key="ErrorText" Color="#FFE74C3C"/>
    <SolidColorBrush x:Key="WarningText" Color="#FFD4A017"/>
    <SolidColorBrush x:Key="WarningHighlight" Color="#FFFF6B6B"/>
    <SolidColorBrush x:Key="WarningBoxBg" Color="#3D2D00"/>
    <SolidColorBrush x:Key="WarningBoxBorder" Color="#FFB400"/>

    <!-- === ANALYSIS MODULE COLORS (% Complete ranges) === -->
    <SolidColorBrush x:Key="AnalysisRedBg" Color="#FFDC3545"/>       <!-- 0-25% -->
    <SolidColorBrush x:Key="AnalysisOrangeBg" Color="#FFFD7E14"/>    <!-- >25-50% -->
    <SolidColorBrush x:Key="AnalysisYellowBg" Color="#FFFFC107"/>    <!-- >50-75% -->
    <SolidColorBrush x:Key="AnalysisGreenBg" Color="#FF28A745"/>     <!-- >75-100% -->
    <SolidColorBrush x:Key="WarningHighlightBackground" Color="#44FF6B6B"/>

    <!-- === UI ELEMENT COLORS === -->
    <SolidColorBrush x:Key="SidebarBackground" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="SidebarBorder" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="DividerColor" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="SplitterDots" Color="#FFAAAAAA"/>
    <SolidColorBrush x:Key="DisabledText" Color="#FFB0B0B0"/>

    <!-- === TOOLBAR BUTTON COLORS === -->
    <SolidColorBrush x:Key="ToolbarForeground" Color="#E8EAED"/>
    <SolidColorBrush x:Key="ToolbarHoverBackground" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="ToolbarHoverForeground" Color="#FFFFFF"/>

    <!-- === GRID HEADER COLORS === -->
    <SolidColorBrush x:Key="GridHeaderForeground" Color="#FFB0B0B0"/>
    <Color x:Key="FilterIconColor">#FFB0B0B0</Color>
    <Color x:Key="FilterIconActiveColor">#FFFF4444</Color>

    <!-- === ACTION BUTTON COLORS === -->
    <SolidColorBrush x:Key="ActionButtonForeground" Color="#E8EAED"/>
    <SolidColorBrush x:Key="ActionFilterForeground" Color="#E8EAED"/>

    <!-- === ELEVATION/SHADOW === -->
    <SolidColorBrush x:Key="SidebarButtonBorder" Color="#FFAAAAAA"/>
    <SolidColorBrush x:Key="ButtonHoverForeground" Color="#FF0078D7"/>
    <Color x:Key="ButtonShadowColor">#000000</Color>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="ButtonShadowOpacity">0.15</sys:Double>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="TextShadowDepth">3</sys:Double>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="TextShadowBlurRadius">1</sys:Double>
    <Color x:Key="TextShadowColor">#CCCCCC</Color>
    <SolidColorBrush x:Key="SummaryLabelColor" Color="#FF1A1A1A"/>
    <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="TextShadowOpacity">0.3</sys:Double>

    <!-- === SPLASH SCREEN === -->
    <SolidColorBrush x:Key="SplashBackground" Color="#002234"/>
    <SolidColorBrush x:Key="SplashForeground" Color="#E8EAED"/>
    <SolidColorBrush x:Key="SplashSpinner" Color="#E8EAED"/>

    <!-- ========================================= -->
    <!-- === DEFAULT BUTTON STYLE === -->
    <!-- ========================================= -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyPrimary}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Focusable="False"
                                    RecognizesAccessKey="True"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ControlHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ControlBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ========================================= -->
    <!-- === TOOLBAR BUTTON STYLE === -->
    <!-- ========================================= -->
    <Style x:Key="ToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ToolbarForeground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyPrimary}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Focusable="False"
                                    RecognizesAccessKey="True"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ToolbarHoverBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource ToolbarHoverForeground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
