<Window x:Class="VANTAGE.Dialogs.GenerateProgressBookDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generate Progress Book"
        Height="440"
        Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundColor}"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Progress Book Info -->
        <Border Grid.Row="0"
                Background="{DynamicResource ControlBackground}"
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Progress Book"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="{DynamicResource ForegroundColor}"
                           Margin="0,0,0,8"/>
                <TextBlock x:Name="txtBookName"
                           Text="Loading..."
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{DynamicResource AccentColor}"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="txtFilterInfo"
                           Text=""
                           FontSize="11"
                           Foreground="{DynamicResource TextColorSecondary}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Summary Stats -->
        <Border Grid.Row="2"
                Background="{DynamicResource ControlBackground}"
                BorderBrush="{DynamicResource ControlBorder}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Summary"
                           FontWeight="SemiBold"
                           FontSize="13"
                           Foreground="{DynamicResource ForegroundColor}"
                           Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="4"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0"
                               Text="Record Count:"
                               Foreground="{DynamicResource TextColorSecondary}"/>
                    <TextBlock x:Name="txtRecordCount"
                               Grid.Row="0" Grid.Column="1"
                               Text="0"
                               Foreground="{DynamicResource ForegroundColor}"
                               HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="2" Grid.Column="0"
                               Text="Estimated Pages:"
                               Foreground="{DynamicResource TextColorSecondary}"/>
                    <TextBlock x:Name="txtEstimatedPages"
                               Grid.Row="2" Grid.Column="1"
                               Text="0"
                               Foreground="{DynamicResource ForegroundColor}"
                               HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="4" Grid.Column="0"
                               Text="Project:"
                               Foreground="{DynamicResource TextColorSecondary}"/>
                    <TextBlock x:Name="txtProjectId"
                               Grid.Row="4" Grid.Column="1"
                               Text="-"
                               Foreground="{DynamicResource ForegroundColor}"
                               HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Status Message -->
        <Border x:Name="statusBorder"
                Grid.Row="3"
                Background="{DynamicResource WarningBoxBg}"
                BorderBrush="{DynamicResource WarningBoxBorder}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,10,0,10"
                Visibility="Collapsed">
            <TextBlock x:Name="txtStatus"
                       Text=""
                       Foreground="{DynamicResource WarningBoxBorder}"
                       FontSize="12"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="btnGenerate"
                    Content="Generate PDF"
                    Width="110"
                    Height="32"
                    Margin="0,0,10,0"
                    Background="{DynamicResource ControlBackground}"
                    Foreground="{DynamicResource StatusGreen}"
                    BorderBrush="{DynamicResource ControlBorder}"
                    Click="BtnGenerate_Click"
                    IsEnabled="False"
                    ToolTip="Generate the PDF and save to file"/>
            <Button x:Name="btnCancel"
                    Content="Cancel"
                    Width="80"
                    Height="32"
                    Background="{DynamicResource ControlBackground}"
                    Foreground="{DynamicResource ForegroundColor}"
                    BorderBrush="{DynamicResource ControlBorder}"
                    Click="BtnCancel_Click"
                    ToolTip="Cancel and close"/>
        </StackPanel>
    </Grid>
</Window>
