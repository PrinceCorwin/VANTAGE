<Window x:Class="VANTAGE.Dialogs.UninstallDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uninstall VANTAGE: Milestone"
        Height="280"
        Width="450"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Warning header -->
        <TextBlock Grid.Row="0"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource ErrorText}">
            Uninstall VANTAGE: Milestone?
        </TextBlock>

        <!-- Description -->
        <TextBlock Grid.Row="2"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource ForegroundColor}"
                   FontSize="13"
                   LineHeight="22">
            This will remove the following from your computer:
        </TextBlock>

        <StackPanel Grid.Row="3" Margin="12,6,0,0">
            <TextBlock Foreground="{StaticResource TextColorSecondary}" FontSize="13" Text="&#x2022;  Application files"/>
            <TextBlock Foreground="{StaticResource TextColorSecondary}" FontSize="13" Text="&#x2022;  Desktop shortcut" Margin="0,2,0,0"/>
            <TextBlock Foreground="{StaticResource TextColorSecondary}" FontSize="13" Text="&#x2022;  Windows registry entries" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Keep data checkbox -->
        <CheckBox x:Name="chkKeepData"
                  Grid.Row="4"
                  Content="Keep local data (database and settings)"
                  IsChecked="True"
                  Foreground="{StaticResource ForegroundColor}"
                  FontSize="13"
                  ToolTip="If unchecked, the local database and all settings will be permanently deleted"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="90"
                    Height="32"
                    Margin="0,0,10,0"
                    Background="{StaticResource ControlBackground}"
                    Foreground="{StaticResource ForegroundColor}"
                    BorderBrush="{StaticResource ControlBorder}"
                    BorderThickness="1"
                    Click="BtnCancel_Click"
                    IsCancel="True"/>
            <Button Content="Uninstall"
                    Width="90"
                    Height="32"
                    Background="{StaticResource ButtonDangerBackground}"
                    Foreground="{StaticResource ForegroundColor}"
                    BorderBrush="{StaticResource ButtonDangerBorder}"
                    BorderThickness="1"
                    Click="BtnUninstall_Click"/>
        </StackPanel>
    </Grid>
</Window>
