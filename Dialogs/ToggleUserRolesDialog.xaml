<Window x:Class="VANTAGE.Dialogs.ToggleUserRolesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Toggle User Roles"
        Height="400"
        Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundColor}"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Toggle User Roles"
                       FontWeight="SemiBold"
                       FontSize="16"
                       Foreground="{DynamicResource ForegroundColor}"/>
            <TextBlock Text="Select a user, then toggle their role."
                       FontSize="12"
                       Foreground="{DynamicResource ForegroundColor}"
                       Opacity="0.7"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- User List -->
        <ListBox x:Name="lstUsers"
                 Grid.Row="2"
                 Background="{DynamicResource ControlBackground}"
                 Foreground="{DynamicResource ForegroundColor}"
                 BorderBrush="{DynamicResource BorderColor}"
                 BorderThickness="1"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="btnToggleAdmin"
                    Content="Toggle Admin"
                    Width="110"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="BtnToggleAdmin_Click"
                    ToolTip="Grant or revoke Admin role for the selected user"/>
            <Button x:Name="btnToggleEstimator"
                    Content="Toggle Estimator"
                    Width="130"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="BtnToggleEstimator_Click"
                    ToolTip="Grant or revoke Estimator (Takeoff access) role for the selected user"/>
            <Button x:Name="btnClose"
                    Content="Close"
                    Width="80"
                    Height="30"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
